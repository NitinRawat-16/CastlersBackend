namespace castlers.Common.Enums
{
    public enum Status
    {
        error,
        success,
        failed
    }
    public enum TenderStatus
    {
        Pending = 1,
        Active = 2,
        Completed = 3,
        Rejected = 4,
        Discarded = 5
    }

    public enum ElectionStatus
    {
        Active,
        Completed
    }
}
